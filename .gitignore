# Binaries / build
*.exe
*.out
/dist/
/build/
# Scripts (ephemeral per project policy)
.scripts/
# Coverage
coverage*.out
coverage*.html
# Local memory
.remember/
# Windows reserved-device-name phantom (from tooling redirects to `nul`)
nul
